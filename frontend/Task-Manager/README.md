## Backend Progress (Day X)

### Completed
- MongoDB configuration using Mongoose
- User and Task schema definitions
- Authentication APIs (Sign Up, Login, Get Profile)
- JWT-based route protection
- Profile image upload using Multer
- User management APIs (Get all users, Get user by ID)

git add dbConfig.js
git commit -m "Configure MongoDB connection"

git add models/
git commit -m "Add User and Task schemas"

git add routes/
git commit -m "Implement authentication APIs"
